<%= simple_form_for @company do |f| %>
  <%= f.input :name %>
  <%= f.input :url %>
  <%= url_for(@company).logo unless @company.new_record? && !@company.logo.nil? %>
  <%= f.input :logo %>
  <%= f.button :submit, class: "btn btn-primary btn-block" %>
<% end %>